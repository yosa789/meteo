name: Test Meteo Script

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '50 23 * * *'  # 🔥 Exécute tous les jours à 00h50 heure française (23h50 UTC)

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Récupérer le code
        uses: actions/checkout@v3

      - name: Installer Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.9'

      - name: Installer les dépendances
        run: |
          pip install selenium webdriver-manager pandas openpyxl

      - name: Exécuter le script
        run: python m.py

      - name: Sauvegarder les fichiers générés
        uses: actions/upload-artifact@v4  # 🔥 Mise à jour vers v4
        with:
          name: wind_data
          path: wind_data_3.xlsx  # Fichier Excel généré
